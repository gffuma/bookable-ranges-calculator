{
  "name": "bookable-ranges-calculator",
  "version": "0.0.1",
  "description": "Calculate bookable ranges of time in λ way with moment.js",
  "scripts": {
    "clean": "rimraf lib && mkdirp lib",
    "build": "npm run clean && babel src -d lib"
  },
  "main": "./dist/bookable-ranges-calculator.js",
  "dependencies": {
    "moment": "^2.10.6",
    "ramda": "^0.18.0"
  },
  "devDependencies": {
    "babel-cli": "^6.3.17",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.3.13",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.5.0"
  }
}
